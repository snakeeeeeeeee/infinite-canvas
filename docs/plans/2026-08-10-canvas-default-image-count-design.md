# Canvas 默认生图张数设计

## 规则

- 未配置画布专用生图张数时默认生成 1 张，与生图工作台的全局默认一致。
- 创建配置节点、从节点发起生图和设置面板均读取同一个 `canvasImageCount` 默认值。
- 持久化配置缺少或清空该字段时回填 1。
- 用户已经明确保存的有效数量继续保留，不覆盖为 1。

## 验收

- 新浏览器首次进入画布时配置节点显示 1 张。
- 没有 `canvasImageCount` 的持久化配置恢复后为 1 张。
- 明确保存为 3 张后仍生成 3 张。
